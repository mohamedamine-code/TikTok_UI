import 'package:flutter/material.dart';

class userExplore extends StatelessWidget {
  const userExplore({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('page Explore'),),
    );
  }
}